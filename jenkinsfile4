pipeline {
    agent any

    stages {
        stage('ChekOut-SCM') {
            steps {
                cleanWs()
                git branch: 'main', credentialsId: 'bskiaku', url: 'https://github.com/bskiaku/b3depogithub.git'
            }
        }
        stage('Build-image-apache2') {
            steps {
                sh 'docker build -t mon-apache2 .'
            }
        }
    }
}
